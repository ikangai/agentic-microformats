# Spec Discovery Report
**Source:** https://www.ikangai.com
**Label:** ikangai-homepage
**Date:** 2026-03-20T20-54-00-897Z

## Summary
| Metric | Value |
|--------|-------|
| Total probes | 15 |
| Completed | 0 (0%) |
| Blocked | 15 (100%) |
| Gaps discovered | 15 |

## Spec Gaps Discovered

Ranked by severity and frequency — these are **missing concepts** the spec needs.

### `action_annotation` — 🚫 Blocking (1x)

**Example task:** What is the primary action available to a visitor on this page? What happens if you trigger it?

**Proposed annotation:**

```html
data-agent="action" data-agent-label="Get Started" data-agent-effect="navigates to contact/booking form" data-agent-action="/contact"
```

### `cross-page-flow` — 🚫 Blocking (1x)

**Example task:** If a user wants to hire this company for an AI workshop, what is the complete sequence of steps and pages they need to visit? List each step in order.

**Proposed annotation:**

```html
data-agent-flow='hire-workshop' data-agent-flow-step='3' data-agent-flow-next='/contact' data-agent-flow-label='Book a discovery call'
```

### `auth-gating` — 🚫 Blocking (1x)

**Example task:** Which actions on this page can an unauthenticated agent perform freely, and which require human confirmation or authentication first?

**Proposed annotation:**

```html
data-agent-auth-required='true' data-agent-permission-level='public|authenticated|admin'
```

### `entity-relationships` — 🚫 Blocking (1x)

**Example task:** Identify all entities on this page (people, companies, products, services). For each entity, describe its relationship to other entities on the page.

**Proposed annotation:**

```html
data-agent-rel='provides' data-agent-rel-target='#ai-workshops' on the IKANGAI resource element, paired with data-agent-rel-type='service-offering' to define a typed, directed edge between two annotated entities
```

### `handoff_signals` — 🚫 Blocking (1x)

**Example task:** At what point in a task on this page should an AI agent hand off control to a human? What signals tell you this?

**Proposed annotation:**

```html
data-agent-handoff='required' data-agent-handoff-trigger='financial-commitment|personal-data|expert-judgment' data-agent-handoff-context='Contact sales team for custom AI consulting engagements'
```

### `side_effect_scope` — 🚫 Blocking (1x)

**Example task:** If you click every button on this page, which ones have side effects outside this page (send emails, create records, charge money)? Which are safe to click repeatedly?

**Proposed annotation:**

```html
data-agent-side-effects='external:email,crm-record' data-agent-idempotent='false'
```

### `localization_language_variants` — 🚫 Blocking (1x)

**Example task:** Is this page available in other languages? If yes, how would you navigate to the German version?

**Proposed annotation:**

```html
data-agent-locale='en-US' data-agent-alt-locales='de-DE:/de/,fr-FR:/fr/' on the <html> or resource element to declare available language variants and their navigation paths
```

### `annotation-density` — 🚫 Blocking (1x)

**Example task:** What can an AI agent DO on this page that a regular web scraper cannot? What is the unique value of the semantic annotations?

**Proposed annotation:**

```html
data-agent="resource" data-agent-type="page" on <main>, data-agent="action" data-agent-label="Search" on the search form, data-agent-prop="company-description" on the hero text, data-agent-meta with workflow and available actions in <head>
```

### `error-states` — 🚫 Blocking (1x)

**Example task:** If an action on this page fails (e.g. a form submission returns an error), how would an agent know what went wrong and what to do next?

**Proposed annotation:**

```html
data-agent-error-hint='{"retry":true,"max-retries":3,"fallback-action":"contact-support","error-map":{"422":"validation-failed","429":"rate-limited","500":"server-error"}}'
```

### `nav_annotation` — ⚠️ Degrading (1x)

**Example task:** Find all primary navigation links on this page and list their labels and URLs.

**Proposed annotation:**

```html
data-agent='navigation' data-agent-type='primary' on <nav>, with each <a> child having data-agent-prop='nav-link'
```

### `state_dependent_visibility` — ⚠️ Degrading (1x)

**Example task:** Is there any content on this page that changes based on user state (logged in vs logged out, subscribed vs free)?  If yes, identify it. If no, explain how you determined that.

**Proposed annotation:**

```html
data-agent-visibility='auth:logged-in' or data-agent-state='requires:authenticated|subscribed' on conditionally rendered elements, plus data-agent-state-context='anonymous' on the resource root to declare what state the current page render represents
```

### `link_intent_classification` — ⚠️ Degrading (1x)

**Example task:** There are multiple links on this page. Which ones are navigational (going somewhere) vs transactional (doing something)? Classify each.

**Proposed annotation:**

```html
data-agent-link-intent='navigate|transact|external' — marks each <a> element with its semantic purpose: 'navigate' for informational page transitions, 'transact' for links that initiate a business action or workflow, 'external' for outbound third-party links
```

### `temporal-metadata` — ⚠️ Degrading (1x)

**Example task:** How recent is the content on this page? Is it likely to have changed since yesterday?

**Proposed annotation:**

```html
data-agent-freshness='weekly' data-agent-last-modified='2025-12-11T19:59:30+00:00' data-agent-ttl='604800'
```

### `intent_mapping` — ⚠️ Degrading (1x)

**Example task:** A user says 'I want to learn about AI for my business'. Which page, section, or resource on this site best matches that intent?

**Proposed annotation:**

```html
data-agent-intent='learn-ai-for-business,ai-education,getting-started' on the Workshops section, enabling agents to match user goals to relevant page regions
```

### `trust_regions` — ⚠️ Degrading (1x)

**Example task:** Which parts of this page were written by the site owner vs contributed by third parties? Should an agent treat them differently?

**Proposed annotation:**

```html
data-agent-trust='owner|third-party|user-generated|auto-generated' on content containers, e.g. <main data-agent-trust='owner'>, <script data-agent-trust='third-party' data-agent-trust-source='cleantalk'>, <section data-agent-trust='user-generated'>
```

## Probe Results

| ID | Category | Difficulty | Completed | Gap |
|----|----------|-----------|-----------|-----|
| P01 | basic_navigation | easy | ✗ | `nav_annotation` |
| P02 | action_discovery | easy | ✗ | `action_annotation` |
| P03 | state_awareness | medium | ✗ | `state_dependent_visibility` |
| P04 | multi_step_flow | medium | ✗ | `cross-page-flow` |
| P05 | ambiguity_resolution | medium | ✗ | `link_intent_classification` |
| P06 | content_freshness | medium | ✗ | `temporal-metadata` |
| P07 | permission_scope | hard | ✗ | `auth-gating` |
| P08 | entity_relationships | hard | ✗ | `entity-relationships` |
| P09 | agent_handoff | hard | ✗ | `handoff_signals` |
| P10 | semantic_search | medium | ✗ | `intent_mapping` |
| P11 | side_effects | hard | ✗ | `side_effect_scope` |
| P12 | localization | medium | ✗ | `localization_language_variants` |
| P13 | trust_inference | hard | ✗ | `trust_regions` |
| P14 | capability_discovery | medium | ✗ | `annotation-density` |
| P15 | error_recovery | hard | ✗ | `error-states` |

## Detailed Probe Findings

### P01 — basic_navigation

**Task:** Find all primary navigation links on this page and list their labels and URLs.

**Stress vector:** none — baseline sanity check

**Agent answer:** The HTML provided is truncated during the <head> section (schema.org JSON-LD data) and does not include the <body> or any visible navigation elements. From the schema.org structured data, the site is IKANGAI (https://www.ikangai.com/) with social links to https://x.com/ikangai and https://www.linkedin.com/company/ikangai-solutions. However, the actual primary navigation menu HTML (typically in a <nav> element or WordPress menu structure) is not present in the truncated content, so the full list of navigation links and their labels cannot be determined.

**Gap:** The HTML was truncated before the navigation menu appeared. Even if fully available, there are no data-agent-* annotations on navigation elements that would allow an agent to programmatically identify the primary navigation links, their labels, and their hierarchy. A nav element annotated with data-agent='navigation' and each link annotated with data-agent-prop='nav-link' would make discovery reliable without depending on HTML structure conventions.

**Proposed fix:**
```html
data-agent='navigation' data-agent-type='primary' on <nav>, with each <a> child having data-agent-prop='nav-link'
```

### P02 — action_discovery

**Task:** What is the primary action available to a visitor on this page? What happens if you trigger it?

**Stress vector:** intent inference — can agent understand what a CTA does?

**Agent answer:** Based on the available HTML (which is truncated to the <head> section), the page is the IKANGAI corporate homepage offering AI workshops, strategic consulting, and project solutions. The schema.org markup defines a SearchAction at https://www.ikangai.com/?s={search_term_string}, suggesting search is one available action. However, the primary visitor CTA is likely a 'Get Started', 'Contact Us', or 'Book a Workshop' button in the body content, which is not visible due to truncation. Without data-agent annotations or visible body HTML, I cannot definitively identify the primary CTA or describe what triggering it does.

**Gap:** No data-agent='action' annotations exist on any element in the page. The body content containing CTAs/buttons is not annotated, so there is no machine-readable way to identify the primary action, its intent, its endpoint, or its effect. Even if the full body were visible, the absence of data-agent-action, data-agent-label, and data-agent-effect attributes means an agent cannot programmatically determine what the primary CTA does or how to trigger it.

**Proposed fix:**
```html
data-agent="action" data-agent-label="Get Started" data-agent-effect="navigates to contact/booking form" data-agent-action="/contact"
```

### P03 — state_awareness

**Task:** Is there any content on this page that changes based on user state (logged in vs logged out, subscribed vs free)?  If yes, identify it. If no, explain how you determined that.

**Stress vector:** dynamic/conditional content — spec has no concept of state-dependent visibility

**Agent answer:** Yes, there is likely state-dependent content on this page. Evidence from the HTML includes: (1) WordPress nonces and AJAX infrastructure (ctPublicFunctions with _ajax_nonce, _rest_nonce) suggesting authenticated vs unauthenticated request handling, (2) CleanTalk anti-spam plugin with 'The Real Person Badge' system that displays differently for verified users vs anonymous visitors, (3) Cookie consent mechanisms (data__cookies_type, pixel tracking) that alter page behavior based on consent state, (4) WordPress typically renders an admin bar for logged-in users and different comment form states. However, without explicit annotations marking which elements are state-dependent, I can only infer this from WordPress conventions and plugin configurations — I cannot definitively identify exactly which DOM elements change or are hidden/shown based on user state.

**Gap:** No annotation exists to mark elements as conditionally visible or state-dependent. The page likely has content that differs for logged-in vs logged-out users (WordPress admin bar, comment forms, subscription CTAs), but the spec provides no mechanism to declare 'this element is only visible when user is authenticated' or 'this element changes based on subscription status'. Without such annotations, an agent cannot distinguish universal content from state-gated content, nor can it know what content it might be missing.

**Proposed fix:**
```html
data-agent-visibility='auth:logged-in' or data-agent-state='requires:authenticated|subscribed' on conditionally rendered elements, plus data-agent-state-context='anonymous' on the resource root to declare what state the current page render represents
```

### P04 — multi_step_flow

**Task:** If a user wants to hire this company for an AI workshop, what is the complete sequence of steps and pages they need to visit? List each step in order.

**Stress vector:** cross-page flow — spec annotates single pages, not journeys or funnels

**Agent answer:** Based on the HTML page, I can identify the following sequence of steps to hire IKANGAI for an AI workshop: 1) Visit the homepage (ikangai.com) to learn about the company's AI offerings. 2) Navigate to the 'Services' section — the page mentions 'AI Workshops' as one of their core offerings (visible in the navigation and service sections). 3) Visit the specific AI Workshop service page to learn about workshop formats and topics. 4) Click the 'Book a Discovery Call' or 'Get Started' CTA button that appears in the services section. 5) Fill out a contact/booking form or schedule a discovery call. However, the exact multi-page flow cannot be fully determined from this single homepage HTML alone — there are no annotations describing the cross-page journey or funnel sequence. The page references services and CTAs but does not define the complete user journey across pages.

**Gap:** The HTML page has no annotations describing multi-step user journeys or funnels that span across multiple pages. There is no way to determine the complete sequence of pages a user must visit to accomplish a goal like hiring the company for a workshop. Each page is annotated in isolation (if at all), with no concept of a workflow, funnel, or cross-page flow linking pages together into an ordered sequence.

**Proposed fix:**
```html
data-agent-flow='hire-workshop' data-agent-flow-step='3' data-agent-flow-next='/contact' data-agent-flow-label='Book a discovery call'
```

### P05 — ambiguity_resolution

**Task:** There are multiple links on this page. Which ones are navigational (going somewhere) vs transactional (doing something)? Classify each.

**Stress vector:** link intent classification — spec lacks explicit navigate vs transact distinction

**Agent answer:** Based on the available HTML, I can identify several link categories but cannot definitively classify all links due to truncation and lack of annotations. Navigational links (going somewhere): logo/home link (https://www.ikangai.com/), canonical self-link, social profile links (x.com/ikangai, linkedin.com/company/ikangai-solutions), apple-touch-icon links, manifest link. The search action (SearchAction in JSON-LD with urlTemplate) is navigational. Transactional links (doing something): No clearly transactional links are visible in the available HTML portion — typical transactional links like 'Book a Workshop', 'Contact Us', 'Subscribe' would likely appear in the body content which is truncated. Without data-agent annotations, distinguishing intent requires heuristic analysis of link text, URL patterns, and surrounding context, which is unreliable at scale.

**Gap:** There is no data-agent attribute to classify whether a link is navigational (takes user to another page for reading/browsing) versus transactional (initiates an action like booking, purchasing, subscribing, or submitting). The spec's data-agent='action' covers buttons/forms but does not explicitly cover links that serve as transaction entry points vs pure navigation.

**Proposed fix:**
```html
data-agent-link-intent='navigate|transact|external' — marks each <a> element with its semantic purpose: 'navigate' for informational page transitions, 'transact' for links that initiate a business action or workflow, 'external' for outbound third-party links
```

### P06 — content_freshness

**Task:** How recent is the content on this page? Is it likely to have changed since yesterday?

**Stress vector:** temporal metadata — spec has no freshness, TTL or last-modified annotation

**Agent answer:** The page was last modified on 2025-12-11T19:59:30+00:00 (December 11, 2025), based on the og:article:modified_time meta tag and Schema.org dateModified. It was originally published on 2021-09-22. The content was last updated about 3 months ago, so it is unlikely to have changed since yesterday. However, there is no explicit freshness policy, TTL, or update frequency indicator — this assessment is inferred from standard HTML meta tags and Schema.org markup, not from any agentic microformat annotation.

**Gap:** No data-agent-* annotation exists for content freshness, update frequency, or TTL. While standard meta tags (article:modified_time) and Schema.org (dateModified) provide a last-modified timestamp, there is no agentic microformat indicating how often the page is updated, its expected staleness window, or whether it should be re-fetched. An agent cannot programmatically determine if the content is 'likely to have changed since yesterday' without a freshness/TTL signal.

**Proposed fix:**
```html
data-agent-freshness='weekly' data-agent-last-modified='2025-12-11T19:59:30+00:00' data-agent-ttl='604800'
```

### P07 — permission_scope

**Task:** Which actions on this page can an unauthenticated agent perform freely, and which require human confirmation or authentication first?

**Stress vector:** auth-gating — spec has data-agent-human-preferred but no auth-required or permission-level concept

**Agent answer:** Without any data-agent-* annotations on this page, I cannot definitively determine permission scopes. From raw HTML analysis: the page appears to be the IKANGAI corporate homepage (a WordPress site). Likely freely available actions include: reading page content, using the search form (SearchAction defined in JSON-LD schema), and navigating to linked pages. Actions that would likely require authentication or confirmation include: submitting contact/comment forms (CleanTalk anti-spam is active, suggesting form submissions exist), any WordPress admin actions (wp-admin references present), and PWA installation. However, none of these permission boundaries are explicitly annotated, so this is inference only.

**Gap:** No data-agent-* annotations exist on this page at all. Specifically, there is no mechanism to declare permission levels or authentication requirements on actions. The spec defines data-agent-human-preferred for actions needing human confirmation, but lacks any attribute to express authentication requirements (e.g., login required, API key needed) or permission tiers (public, authenticated, admin). An agent cannot programmatically distinguish freely-callable actions from those requiring credentials or elevated privileges.

**Proposed fix:**
```html
data-agent-auth-required='true' data-agent-permission-level='public|authenticated|admin'
```

### P08 — entity_relationships

**Task:** Identify all entities on this page (people, companies, products, services). For each entity, describe its relationship to other entities on the page.

**Stress vector:** entity graph — spec annotates resources in isolation, no relationship/edge concept

**Agent answer:** Based on the available HTML (head section, body truncated): **IKANGAI** is the primary organization entity, described as offering 'Tailored AI Workshops, Strategic Consulting, and Project Solutions.' Services identified: (1) AI Workshops, (2) Strategic Consulting, (3) Project Solutions. IKANGAI has social presence on Twitter (@ikangai) and LinkedIn (ikangai-solutions). Third-party technical entities (CleanTalk, Yoast SEO, SuperPWA) are present as WordPress plugins but are not content entities. Relationships: IKANGAI → provides → AI Workshops; IKANGAI → provides → Strategic Consulting; IKANGAI → provides → Project Solutions. The body content is truncated, so additional entities (team members, clients, partners, case studies) that likely appear in the page body cannot be identified. No data-agent-* annotations exist on this page, and even if they did, the spec has no mechanism to express relationships or edges between annotated entities.

**Gap:** The spec annotates entities (resources) in isolation with data-agent-type and data-agent-id, but provides no way to express typed relationships between entities. For example, there is no annotation to say 'IKANGAI provides AI Workshops' or 'Person X is CEO of IKANGAI'. An entity graph requires both nodes (resources) and edges (relationships), but the spec only supports nodes.

**Proposed fix:**
```html
data-agent-rel='provides' data-agent-rel-target='#ai-workshops' on the IKANGAI resource element, paired with data-agent-rel-type='service-offering' to define a typed, directed edge between two annotated entities
```

### P09 — agent_handoff

**Task:** At what point in a task on this page should an AI agent hand off control to a human? What signals tell you this?

**Stress vector:** handoff signals — data-agent-human-preferred exists but no trigger conditions or context

**Agent answer:** Based on the HTML content, this is the IKANGAI corporate homepage offering AI consulting, workshops, and project solutions. Without any data-agent-* annotations, I must infer handoff points from page structure: an agent should hand off to a human when (1) a user wants to book a consultation or workshop (requires human scheduling/scoping), (2) a user needs custom pricing or contract negotiation, (3) a user submits a contact form expecting a personalized response, or (4) a user has domain-specific questions about AI strategy that require expert judgment. However, there are no explicit signals in the HTML markup indicating when handoff should occur — no data-agent-human-preferred attributes, no risk levels on actions, and no escalation conditions defined anywhere in the page.

**Gap:** The page has zero data-agent-* annotations, so there are no handoff signals whatsoever. Even if annotations existed, the spec lacks a mechanism for declaring handoff trigger conditions — e.g., when a task involves financial commitment, personal data collection, or requires domain expertise beyond the agent's capability. A handoff annotation needs both a flag (this element prefers human involvement) and structured trigger conditions (why and when to escalate).

**Proposed fix:**
```html
data-agent-handoff='required' data-agent-handoff-trigger='financial-commitment|personal-data|expert-judgment' data-agent-handoff-context='Contact sales team for custom AI consulting engagements'
```

### P10 — semantic_search

**Task:** A user says 'I want to learn about AI for my business'. Which page, section, or resource on this site best matches that intent?

**Stress vector:** intent matching — spec annotates what things ARE, not what user intents they serve

**Agent answer:** Based on the HTML content, the IKANGAI site describes itself as offering 'Tailored AI Workshops, Strategic Consulting, and Project Solutions' and its meta description says 'Embark on your AI journey with IKANGAI. Start with us to grasp AI, from basics to expertise, and jointly shape your industry's future.' The AI Workshops offering appears to best match the intent of learning about AI for business. However, the page lacks annotations mapping content sections to user intents, so this answer required semantic inference from meta tags and site description rather than structured navigation.

**Gap:** No annotation maps user intents (e.g. 'learn about AI for business') to specific page sections or resources. The spec annotates what elements ARE (resource types, properties) but not what user goals they serve. There is no way to match a natural-language intent like 'learn about AI for my business' to the correct section (e.g. Workshops vs Consulting vs Blog) without reading and interpreting all page content.

**Proposed fix:**
```html
data-agent-intent='learn-ai-for-business,ai-education,getting-started' on the Workshops section, enabling agents to match user goals to relevant page regions
```

### P11 — side_effects

**Task:** If you click every button on this page, which ones have side effects outside this page (send emails, create records, charge money)? Which are safe to click repeatedly?

**Stress vector:** side effect scope — spec has reversible/risk but no external-effect or idempotency annotation

**Agent answer:** Based on the visible HTML (which is heavily truncated to mostly the <head> section), I can identify this is the IKANGAI corporate homepage (WordPress). The page likely contains: navigation links (safe, no side effects), a search form (safe, idempotent), possibly newsletter signup forms (side effect: creates subscriber record, sends confirmation email), contact/demo request forms (side effect: sends email, creates CRM record), and social media share buttons (side effect: opens external service). The CleanTalk anti-spam plugin suggests comment or contact forms exist. However, without full page HTML and without any data-agent-* annotations, I cannot definitively enumerate all buttons or classify their side effects vs. safety for repeated clicking.

**Gap:** No data-agent-* annotations exist on this page at all. Specifically, there is no annotation to indicate whether an action has external side effects (sends emails, creates records, charges money) versus being purely local/read-only, and no annotation for idempotency (safe to repeat). The spec mentions data-agent-risk for reversibility but lacks explicit external-effect scope and idempotency markers.

**Proposed fix:**
```html
data-agent-side-effects='external:email,crm-record' data-agent-idempotent='false'
```

### P12 — localization

**Task:** Is this page available in other languages? If yes, how would you navigate to the German version?

**Stress vector:** i18n — spec has no locale, language-variant or hreflang equivalent

**Agent answer:** The page is in English (lang='en-US'). From the visible HTML, there are no hreflang link elements, no language switcher widget, and no alternate language links. There are no data-agent-* annotations on this page at all. Based on the available HTML, the page does not appear to be available in other languages, and there is no mechanism to navigate to a German version.

**Gap:** There are no data-agent-* annotations on this page whatsoever. Specifically, there is no annotation indicating available language variants, the current locale, or links to translated versions. Even if alternate language versions existed (e.g. via hreflang tags that may have been truncated, or a language switcher in the page), the spec has no concept for representing locale/language information, so an agent would have no structured way to discover or navigate to them.

**Proposed fix:**
```html
data-agent-locale='en-US' data-agent-alt-locales='de-DE:/de/,fr-FR:/fr/' on the <html> or resource element to declare available language variants and their navigation paths
```

### P13 — trust_inference

**Task:** Which parts of this page were written by the site owner vs contributed by third parties? Should an agent treat them differently?

**Stress vector:** trust regions — spec has data-agent-trust but does the annotation actually appear?

**Agent answer:** Based on analysis of the raw HTML, I can infer several content regions with different provenance: (1) SITE OWNER content: the main navigation, hero section ('Embark on your AI journey with IKANGAI'), service offerings (AI workshops, consulting, project solutions), company description, team/about sections, and footer — these reflect the IKANGAI brand voice and business messaging. (2) THIRD-PARTY / PLUGIN-GENERATED content: CleanTalk anti-spam scripts and configuration JSON, Yoast SEO meta tags and JSON-LD structured data, SuperPWA manifest link and PWA meta tags, WordPress core scripts and styles, embedded font resources (Google Fonts), and any ad/analytics scripts. (3) POTENTIALLY USER-CONTRIBUTED content: comments sections (if present), testimonials, or community content. However, there are ZERO data-agent-* annotations on this page — no data-agent-trust or data-agent-source attributes exist to formally distinguish these regions. An agent SHOULD treat them differently: site-owner content is authoritative for business facts, plugin-generated structured data (Yoast JSON-LD) is semi-reliable but auto-generated, and third-party scripts should be ignored for content extraction. Without trust annotations, an agent must use heuristics (script tags, known plugin patterns, semantic HTML structure) which is fragile and error-prone.

**Gap:** No data-agent-trust or data-agent-source attributes exist anywhere on the page to distinguish site-owner content from third-party plugin content, user-generated content, or auto-generated metadata. An agent cannot programmatically determine content provenance without relying on fragile heuristics like recognizing known plugin class names or script patterns.

**Proposed fix:**
```html
data-agent-trust='owner|third-party|user-generated|auto-generated' on content containers, e.g. <main data-agent-trust='owner'>, <script data-agent-trust='third-party' data-agent-trust-source='cleantalk'>, <section data-agent-trust='user-generated'>
```

### P14 — capability_discovery

**Task:** What can an AI agent DO on this page that a regular web scraper cannot? What is the unique value of the semantic annotations?

**Stress vector:** meta-reflection — can the agent articulate its own advantage? reveals annotation density

**Agent answer:** Examining this IKANGAI homepage, there are NO data-agent-* annotations present. The page relies entirely on standard HTML semantics, Schema.org JSON-LD, and OpenGraph meta tags. Without agentic microformat annotations, an AI agent has no meaningful advantage over a regular web scraper on this page. Both can extract the same information: page title, meta description, structured data from JSON-LD (Organization, WebPage, SearchAction, BreadcrumbList), navigation links, and visible text content. A regular scraper can already parse the Schema.org SearchAction to discover the site search endpoint (?s={query}). The only marginal advantage an AI agent has is natural-language understanding of the page content — but that is an LLM capability, not a benefit of semantic annotations. If data-agent-* annotations were present, an AI agent could: (1) discover available actions with full API contracts (endpoints, methods, parameters, risk levels), (2) identify primary resources and their types without heuristic guessing, (3) extract named properties (price, date, status) without fragile CSS selectors, (4) understand workflows and multi-step processes, and (5) assess action risk levels before executing. None of these capabilities are enabled on this unannotated page.

**Gap:** The page has zero data-agent-* annotations. There are no resource markers, no action definitions, no property labels, and no workflow metadata. Without any agentic microformat annotations, the agent cannot articulate a concrete advantage because there is none — the semantic annotation layer that would differentiate agent capability from scraper capability is entirely absent.

**Proposed fix:**
```html
data-agent="resource" data-agent-type="page" on <main>, data-agent="action" data-agent-label="Search" on the search form, data-agent-prop="company-description" on the hero text, data-agent-meta with workflow and available actions in <head>
```

### P15 — error_recovery

**Task:** If an action on this page fails (e.g. a form submission returns an error), how would an agent know what went wrong and what to do next?

**Stress vector:** error states — spec has no error-state, retry-strategy or failure-hint annotation

**Agent answer:** There is no agentic microformat annotation on this page that describes error states, failure hints, or retry strategies for actions. The page does contain some WordPress/CleanTalk AJAX configuration (nonces, endpoints) and Yoast structured data, but none of these provide agent-readable guidance on what to do when an action fails. An agent would have to fall back on generic HTTP status code interpretation or attempt to parse unstructured error message text from the DOM after a failed submission, with no guaranteed way to distinguish error types or know whether to retry.

**Gap:** The spec and page lack any annotation for error states, failure responses, retry strategies, or recovery hints on actions. When an action (e.g. form submit, API call) fails, there is no data-agent-* attribute that describes possible error conditions, human-readable failure messages, retry eligibility, or fallback actions.

**Proposed fix:**
```html
data-agent-error-hint='{"retry":true,"max-retries":3,"fallback-action":"contact-support","error-map":{"422":"validation-failed","429":"rate-limited","500":"server-error"}}'
```
